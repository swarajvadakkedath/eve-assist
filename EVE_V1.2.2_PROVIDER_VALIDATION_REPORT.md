# EVE v1.2.2 — Provider Validation Report

> **Status**: TEMPLATE — run `python validate_providers.py` to populate with live data.
>
> **Generated**: (auto-populated by validate_providers.py)
>
> **Backend**: http://127.0.0.1:8456

---

## Executive Summary

| Metric | Value |
|--------|-------|
| Backend Reachable | (auto) |
| Providers Configured | (auto) |
| Providers Healthy | (auto) |
| Providers Offline | (auto) |
| Total Models | (auto) |
| Free Models | (auto) |
| Commercial Policy | (auto) |
| Routing OK | (auto) |
| Security | (auto) |
| Final Verdict | (auto) |

---

## Provider Matrix

| Provider | Connection | Models | Free | Chat | Stream | Vision | Tools | JSON | Reasoning | Health | Latency | Verdict |
|----------|-----------|--------|------|------|--------|--------|-------|------|-----------|--------|---------|---------|
| (auto-populated) | | | | | | | | | | | | |

### Verdict Legend

| Verdict | Meaning |
|---------|---------|
| **LIVE VERIFIED** | Provider connected, models discovered, chat test passed |
| **EXPECTED OFFLINE** | Provider disabled or not configured — expected behavior |
| **EXTERNAL FAILURE** | Provider returned auth/connect error — check API key |
| **INTEGRATION FAILURE** | Provider connected but chat or model discovery failed |

---

## Model Inventory

| Category | Count |
|----------|-------|
| Total Models | (auto) |
| Free Models | (auto) |
| Free Tier Models | (auto) |
| Paid Models | (auto) |
| Unknown Classification | (auto) |
| Reasoning Models | (auto) |
| Vision Models | (auto) |
| Embedding Models | (auto) |
| Streaming-Capable Providers | (auto) |

---

## Model Details by Provider

### (auto-populated per provider)

| Model ID | Free | Vision | Reasoning | Tools | JSON | Context |
|----------|------|--------|-----------|-------|------|---------|
| (auto) | | | | | | |

---

## Capability Matrix

| Capability | Providers Supporting | Models |
|-----------|---------------------|--------|
| Vision | (auto) | (auto) |
| Reasoning | (auto) | (auto) |
| Streaming | (auto) | (auto) |
| Tool Calling | (auto) | (auto) |
| JSON Mode | (auto) | (auto) |
| Embeddings | (auto) | (auto) |
| Audio | (auto) | (auto) |
| Images | (auto) | (auto) |

---

## Commercial Policy Validation

| Check | Result |
|-------|--------|
| Policy Type | (auto — FREE_ONLY / NO_DIRECT_PAID / ALLOW_PAID) |
| FREE_ONLY blocks PAID | (auto) |
| FREE_ONLY blocks UNKNOWN | (auto) |
| Free models available per provider | (auto) |

---

## Routing Validation

| Check | Result |
|-------|--------|
| SmartRouter resolving categories | (auto) |
| Correct capability routing | (auto) |
| Fallback chain tested | (auto) |
| Rate-limit handling | (auto) |

---

## Fallback Validation

| Check | Result |
|-------|--------|
| Provider disabled → fallback triggers | (auto) |
| No user-visible failure | (auto) |
| Configuration restored | (auto) |

---

## Health Summary

| Provider | Health Score | Latency | Success Rate | Last Check |
|----------|-------------|---------|-------------|------------|
| (auto) | | | | |

---

## Performance

| Provider | Chat Latency | Streaming Latency |
|----------|-------------|-------------------|
| (auto) | | |

---

## Security

| Check | Result |
|-------|--------|
| Credential Manager only | (auto) |
| No plaintext keys in API responses | (auto) |
| No secrets in providers.json | (auto) |
| No logs containing secrets | (auto) |
| No frontend leakage | (auto) |
| Sanitized error messages | (auto) |

---

## Errors & Warnings

(auto-populated per provider)

---

## Known Limitations

1. Sandbox environment cannot perform live validation — run `validate_providers.py` on local machine
2. Vision testing requires a test image file (not included in automated run)
3. Tool calling testing requires a running backend with tool execution enabled
4. Ollama validation depends on local Ollama service being running
5. Rate limits may cause intermittent failures for rapid sequential testing

---

## Recommendations

(auto-populated based on validation results)

---

## Final Verdict

| Status | Meaning |
|--------|---------|
| **PRODUCTION_READY** | All configured providers authenticated, models discovered, health verified, no security issues |
| **INTEGRATION_FAILURES_DETECTED** | One or more providers have integration failures — review errors section |
| **SECURITY_FAILURES** | Security checks failed — do not release until resolved |

**Verdict**: (auto)

---

*Report generated by validate_providers.py — EVE v1.2.2 release validation tool*
